using Bridge;
using Bridge.React;
using Retyped.Primitive;

namespace Dashboard.Bridge.rt
{
    [External]
    public class Input<P, S> : BaseComponent<P>
    {
        public Input(P props) : base(props)
        {
        }
    }
}